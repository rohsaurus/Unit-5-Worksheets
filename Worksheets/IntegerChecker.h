#pragma once
#include <iostream>
#include <string>

/*
void integercheckertemplate (std::string x, int temp)
{

int count = 0;
    while (true) {
        // Iteratres to every character checking if it is a digit or not
        // this deduces whether user inputted integer or something else
        for (int i = 0; i < x.length(); i++) 
        {
            if (isdigit(x[i]) == false) 
            {
                std::count=1;
                break;
            }
            else
                std::count=0;
     
        } if(std::count==0)
        {
            std::cout << "";
            temp = std::stoi(x);
            if (temp >= 1 && temp <= 8)
            {
                std::cout << " ";
                break;
            }
        }
  
        else
            std::cout << "";
    }
    }
    */

